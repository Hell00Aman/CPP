#include<bits/stdc++.h>   // Includes all standard libraries
using namespace std;      // Use standard namespace

int main(){
    int x, y;             // Declare two integers
    cin >> x >> y;        // Input values
    cout << x * y;        // Print product
    return 0;             // End program
}
