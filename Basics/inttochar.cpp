#include<bits/stdc++.h>   // Includes all standard C++ libraries
using namespace std;      // Use the standard namespace

int main(){
    int x, y;             // Declare two integer variables for input
    cin >> x >> y;        // Read two integers from user input
    cout << (char)x << endl  // Convert first integer to char and print
         << (char)y;          // Convert second integer to char and print
    return 0;             // Exit program successfully
}
