#include<bits/stdc++.h>   // Header file includes all standard libraries
using namespace std;     // Use standard namespace

int main(){
    float x = 2.1, y = 3.0;   // Declare and initialize two float variables
    cout << x * y / 3;        // Multiply x and y, then divide result by 3, print output
    return 0;                 // End program successfully
}
