#include<bits/stdc++.h>   // Includes all standard libraries
using namespace std;      // Use standard namespace

int main() {
    int x = 5;            // Declare integer x and assign 5
    cout << x << endl;    // Print x (5) and go to new line
    
    int x1 = x + 4;       // Declare x1 as x+4 → 9
    cout << x1 << endl;   // Print x1 (9) and go to new line

    return 0;             // End program successfully
}
